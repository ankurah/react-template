let name = variable::get("project-name");

print("");
print("Next steps:");
print("  cd " + name);
print("  ./dev.sh");
print("");
print("dev.sh builds the Rust server and the frontend and starts them on");
print("randomized local ports — it prints the URL to open on startup.");
print("Stop with ./dev.sh --stop  (see ./dev.sh --help for --status/--logs).");
print("");
print("Need help? Join the Ankurah Discord! https://discord.gg/XMUUxsbT5S");
print("");
